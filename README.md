# Bootloader for BR9008-F19

Commands:
`xmodem` - receive a binary file
`update` - place uploaded binary file into RAM
`gop` - start the program from `FLASH_ADDR_APP` (the flash_jump function is in the utils/util.c)
